# XPlane2FLT v1.0.8

- Changes missing diffuse textures from a hard export block to a user-confirmable warning.
- Adds an **Export anyway** action that includes affected geometry without a diffuse texture.
- Keeps corrupt archives, invalid geometry, and unsafe output sizes as blocking errors.
