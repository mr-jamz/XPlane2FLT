# XPlane2FLT v1.0.11

- Keeps attached aircraft geometry visible for inspection instead of converting saved plugin loadout options into forced object-hide datarefs.
- Fixes the MH-60R `seats.obj` attachment being hidden by `seats=0` in `opt_config.ini`.
- Preserves saved `conf/*` values for authored OBJ8 animation branches, including the aircraft's `seats2` interior variant.
- Retains the v1.0.9 DDS orientation fix and v1.0.10 role-aware material handling.
- Does not modify FLT export geometry, UV coordinates, or source textures.
